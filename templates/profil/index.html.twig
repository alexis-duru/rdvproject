{% extends 'base.html.twig' %}

{% block title %}PROFIL{% endblock %}

{% block body %}
<section class="section-profil">
    <div class="container-profil">
        <img src="../assets/img/persona.jpg">
        <h2>{{ app.user.username }}</h2>
        <div class="infos">
            <p>CONTACT</p>
            <p><i class="fas fa-envelope"></i> E-mail : <span>{{ app.user.email }}</span></p>
            <p><i class="fas fa-phone"></i> Phone : <span>{{ app.user.phoneNumber }}</span></p>
            <button class="btn-confirm"><a href="{{path('app_profil_edit')}}">EDIT</a></button>
        </div>
    </div>
</section>
{% endblock %}
