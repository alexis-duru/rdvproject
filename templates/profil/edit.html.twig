{% extends 'base.html.twig' %}

{% block title %}EDIT PROFIL{% endblock %}

{% block body %}
<section class="section-profil">
    <div class="container-profil">
            <img src="../assets/img/persona.jpg">
            <h2>{{ app.user.username }}</h2>
            <div class="infos">
                <p>EDIT</p>
                {{ form_start(form) }}
                {{ form_widget(form)}}
                <button class="btn-confirm">SAVE</a></button>
                {{ form_end(form) }}
            </div>
    </div>
</section>
{% endblock %}
{# <p>CONTACT</p>
<p><i class="fas fa-envelope"></i> E-mail :</p>
<p><i class="fas fa-phone"></i> Phone :</p> #}